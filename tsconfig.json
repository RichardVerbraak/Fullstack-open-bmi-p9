// This is used to define how the TS compiler should interpret the code
// noImplicitAny means every variable has to have a type (so explicitly setting types basically)
{
    "compilerOptions": {
      "noImplicitAny": true,
    }
  }
